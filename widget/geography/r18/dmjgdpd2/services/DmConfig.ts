import {ScaleValue} from '../../../../../src/konva/template/ScaleValue';

export class DmConfig {

  scaleValue = new ScaleValue();

  scale = this.scaleValue.scale;

  bigMapConfig = {
      width :  660 * this.scale,
      height :  460 * this.scale,
      opacity : 1,
      x : (24 + 158) * this.scale,
      y : 74 * this.scale,
  };

  // 左1
  leftOneConfig = {
    width :  144 * this.scale,
    height :  108 * this.scale,
    opacity : 1,
    x : (62 + 158) * this.scale,
    y : 147 * this.scale,
  };

  // 左2
  leftTwoConfig = {
    width :  144 * this.scale,
    height :  108 * this.scale,
    opacity : 1,
    x : (533 + 158) * this.scale,
    y : 148 * this.scale,
  };

  // 左3
  leftThreeConfig = {
    width :  144 * this.scale,
    height :  108 * this.scale,
    opacity : 1,
    x : (254 + 158) * this.scale,
    y : 320 * this.scale,
  };

  // 右1
  rightOneConfig = {
    width :  144 * this.scale,
    height :  108 * this.scale,
    opacity : 1,
    x : (282 + 158) * this.scale,
    y : 140 * this.scale,
  };

  // 右2
  rightTwoConfig = {
    width :  144 * this.scale,
    height :  108 * this.scale,
    opacity : 1,
    x : (46 + 158) * this.scale,
    y : 391 * this.scale,
  };

  // 右3
  rightThreeConfig = {
    width :  144 * this.scale,
    height :  108 * this.scale,
    opacity : 1,
    x : (474 + 158) * this.scale,
    y : 355 * this.scale,
  };

  line1Config = {
    width :  2 * this.scale,
    height :  14 * this.scale,
    rotation: 90,
    x : (215 + 158) * this.scale,
    y : 210 * this.scale,
  };

  line2Config = {
    width :  2 * this.scale,
    height :  14 * this.scale,
    x : (282.5 + 158) * this.scale,
    y : 225 * this.scale,
  };

  line3Config = {
    width :  2 * this.scale,
    height :  14 * this.scale,
    x : (533.5 + 158) * this.scale,
    y : 234 * this.scale,
  };

  line4Config = {
    width :  2 * this.scale,
    height :  14 * this.scale,
    rotation: 0,
    x : (153 + 158) * this.scale,
    y : 380 * this.scale,
  };

  line5Config = {
    width :  2 * this.scale,
    height :  14 * this.scale,
    rotation: 0,
    x : (313 + 158) * this.scale,
    y : 425 * this.scale,
  };

  line6Config = {
    width :  2 * this.scale,
    height :  14 * this.scale,
    x : (475 + 158) * this.scale,
    y : 445 * this.scale,
  };

  button1Config = {
    width :  32 * this.scale,
    height :  32 * this.scale,
    x : 275 * this.scale,
    y : 189 * this.scale,
  };

  button2Config = {
    width :  32 * this.scale,
    height :  32 * this.scale,
    x : 497 * this.scale,
    y : 177 * this.scale,
  };

  button3Config = {
    width :  32 * this.scale,
    height :  32 * this.scale,
    x : 746 * this.scale,
    y : 189 * this.scale,
  };

  button4Config = {
    width :  32 * this.scale,
    height :  32 * this.scale,
    x : 258 * this.scale,
    y : 432 * this.scale,
  };

  button5Config = {
    width :  32 * this.scale,
    height :  32 * this.scale,
    x : 470 * this.scale,
    y : 363 * this.scale,
  };

  button6Config = {
    width :  32 * this.scale,
    height :  32 * this.scale,
    x : 688 * this.scale,
    y : 397 * this.scale,
  };

  labelPointBig1Config = {
    width :  28 * this.scale,
    height :  32 * this.scale,
    x : (214 + 158) * this.scale,
    y : 195 * this.scale,
  };

  labelPointBig2Config = {
    width :  28 * this.scale,
    height :  32 * this.scale,
    x : (242 + 158) * this.scale,
    y : 210 * this.scale,
  };

  labelPointBig3Config = {
    width :  28 * this.scale,
    height :  32 * this.scale,
    x : (493 + 158) * this.scale,
    y : 225 * this.scale,
  };

  labelPointBig4Config = {
    width :  28 * this.scale,
    height :  32 * this.scale,
    x : (140 + 158) * this.scale,
    y : 350 * this.scale,
  };

  labelPointBig5Config = {
    width :  28 * this.scale,
    height :  32 * this.scale,
    x : (300 + 158) * this.scale,
    y : 435 * this.scale,
  };

  labelPointBig6Config = {
    width :  28 * this.scale,
    height :  32 * this.scale,
    x : (436 + 158) * this.scale,
    y : 435 * this.scale,
  };
}







