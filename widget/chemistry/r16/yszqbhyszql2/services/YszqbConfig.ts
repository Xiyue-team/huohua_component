
export class YszqbConfig {

    scale = (window.innerWidth / 1024 - 0.03);
    height = window.innerHeight;
    width = window.innerWidth;

    // 步骤1
    step1Config = {
        width :  580 * this.scale,
        height :  343 * this.scale,
        opacity : 1,
        x : (this.width - 580 * this.scale) / 2,
        y : (this.height - 343 * this.scale) / 2,
    };

    // 步骤2
    step2Config = {
        width :  580 * this.scale,
        height :  343 * this.scale,
        opacity : 1,
        x : (this.width - 580 * this.scale) / 2,
        y : (this.height - 343 * this.scale) / 2,
        visible: false
    };

    // 步骤3
    step3Config = {
        width :  580 * this.scale,
        height :  343 * this.scale,
        opacity : 1,
        x : (this.width - 580 * this.scale) / 2,
        y : (this.height - 343 * this.scale) / 2,
        visible: false
    };

    // 步骤4
    step4Config = {
        width :  580 * this.scale,
        height :  343 * this.scale,
        opacity : 1,
        x : (this.width - 580 * this.scale) / 2,
        y : (this.height - 343 * this.scale) / 2,
        visible: false
    };

}







